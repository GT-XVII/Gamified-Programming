import React, { useState, useEffect } from 'react';  // Importing React and hooks to store and fetch the data 
import axios from 'axios';  // Importing axios to make API requests
import GameBoard from '../components/GameBoard';  // Import the component to display the game board
import GameStatus from '../components/GameStatus';  // Import the component to display the game status

